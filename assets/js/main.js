function P_S(x, y) {
  x = Number(prompt("Enter number 1: "));
  y = Number(prompt("Enter number 2: "));
  P = (x + y) * 2;
  S = x * y;
  alert("Периметер: " + P);
  alert("Площа: " + S);
}

P_S();

a = prompt("Enter number: ");
alert((a + 10) * 20 - 30 / 5);
